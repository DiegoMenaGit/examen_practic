import 'package:flutter/cupertino.dart';
import '../widgets/scan_tile.dart';

class UsuScreen extends StatelessWidget {
  const UsuScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return UsuarisTiles();
  }
}
